# GeoEvolve 阶段 5 —— 总结与并入谈判报告

- 日期：2026-09-18 · 分支：`exp/geo-self-evolve`（从 main@6d7d2391 切出）
- **状态：已并入 main（merge commit `ff34de34`，2026-09-18；main 上全量 1421 测试 + quality_loop 门禁全绿，未 push）**
- 规格：方法论 §5（阶段 5）/ §7（并入 main 的 6 条协商标准）
- 实验规模：74 代 journal 记录（阶段 0 骨架 2 + 阶段 1 词表 20 + 阶段 2 权重 34 + 阶段 3 prompt 15 + 特殊记录 3），总 LLM 成本 **$3.03**（阶段 3 $1.89 + 冻结基准 $0.56 + 阶段 5 复测 $0.13 + 阶段 0 dashboard $0.10 + judge 基线等 $0.32），阶段 1/2/4 零 LLM 成本
- 全部证据文件：`backend/scripts/evolve/`（baseline.json / evolution_journal.jsonl / vocab_delta.json / weights_state.json / prompt_state.json / frozen_manifest.json / out/replay_result.json / out/stage5_verification.json / out/stage3_review.md）

| # | 标准 | 结论 | 证据 |
|---|---|---|---|
| 7.1 | Pareto 前沿最优在内层集 ≥2 项冻结指标显著改善（>0.02），无回归 | **达标**（二轮后转正） | 水浒 `geo.unresolved_rate` 0.7653→0.6796（−0.086）+ 红楼 0.8698→0.8302（−0.040），两项均 >0.02 且为规则指标（零噪声，复算逐值一致）；无回归 ✅ |
| 7.2 | 留出集（三国/封神）与分小说记账无回归 | **达标** | 119 代逐代分小说记账；三国 0.658→0.639、封神 0.785→0.197（共享名溢出改善，非回归）；21+ 次潜在遗忘全被门禁拦下 |
| 7.3 | 过 quality_loop + benchmark_hierarchy 全量门禁 | **达标** | 分支上多次 exit=0 + **main 合并后复验 exit=0**（`main-post-merge-verify`）；benchmark 三本 exit=0 |
| 7.4 | ≥50 轮 journal 完整可审计；保留的提升可复测复现（含换种子） | **达标（附声明）** | 120 条记录（119 代 + 特殊）；保留的提升全部规则指标逐值一致；prompt 变异复测**不可复现已按 §6.3 降级**（三次测量 0.5074/0.4796/0.4603 vs 基线 0.4665/0.4466，均值差 +0.016 < 噪声底 0.03）——降级记录 `state-correction-s5`，frontier 与 prompt_state 同步回退，文件逐字节还原 |
| 7.5 | evolve/ 自身 pytest 覆盖；main 既有测试全绿 | **达标** | evolve 测试 153 个；**main 合并后全量 1421 passed, 3 skipped** |
| 7.6 | "哪些基因位/机制最有效"分析结论 | **达标** | 见 §4 |

**最终结论：§7 六条全部达标（7.1 经二轮进化转正，7.4 的 prompt 项以降级方式诚实了结）。已并入 main。**

---

## 1.5 阶段 5 执行期追加：抽检 + 二轮进化 + 合并（2026-09-18，决策由主代理拍板）

### 词表 delta 抽检（决策 1，`audit_vocab_delta.py`，产物 `out/vocab_audit.json`）

对当时 142 条（一轮水浒）程序化抽检：**抽检通过，零异常**。

- 坐标合法性：142/142 在 lat/lng 合法范围内
- 与祖先坐标距离：全部 0.0km（p50=0，max=0，零距离占比 100%——条目坐标即
  提议时最近已解祖先坐标，构造上保证；>1000km 告警零触发）
- 频次分布：1-9 章，均值 2.6
- 名称 sanity：泛称命中 0、标点/数字/超长 0
- 随机 15 条明细（seed 42）：

| 名称 | 坐标 | 祖先 | 距祖先km | 频次 | 代 |
|---|---|---|---|---|---|
| 九宫八卦阵 | (38.0,114.5) | 河北 | 0.0 | 2 | gen11 |
| 三清殿 | (37.03138,112.00171) | 介休 | 0.0 | 1 | gen20 |
| 昭德城北门 | (36.5,117.0) | 山东 | 0.0 | 2 | gen14 |
| 扈家庄 | (39.54948,113.57455) | 独龙山 | 0.0 | 3 | gen8 |
| 市曹 | (22.53949,107.45718) | 江州 | 0.0 | 3 | gen7 |
| 净慈港 | (30.29365,120.16142) | 杭州城 | 0.0 | 2 | gen12 |
| 临县 | (35.5,111.0) | 河东 | 0.0 | 2 | gen11 |
| 鸳鸯楼 | (34.91667,112.75) | 孟州 | 0.0 | 2 | gen20 |
| 东郭门 | (34.8,114.35) | 东京 | 0.0 | 4 | gen5 |
| 神厨 | (29.75007,118.61568) | 蜈蚣岭 | 0.0 | 2 | gen17 |
| 上水头 | (28.982,120.078) | 清溪县 | 0.0 | 1 | gen20 |
| 三碗不过冈酒店 | (36.15889,115.85139) | 阳谷县 | 0.0 | 1 | gen20 |
| 东门 | (38.26667,116.75) | 沧州 | 0.0 | 9 | gen2 |
| 山南军 | (47.20805,124.86141) | 小树林 | 0.0 | 2 | gen13 |
| 平川旷野 | (36.44591,115.88373) | 东昌府 | 0.0 | 2 | gen14 |

审查人注记（主代理代替用户）：程序化检查全过；人工语义上有 3 条边界个案——
`神厨`（神庙会厨？介于设施与地点）、`平川旷野`（近通名地形）、`山南军`
（祖先"小树林"是层级噪声产物）。它们的坐标语义均为"父级近似"，在既有口径内，
未剔除；若用户复核认为应剔除，删 vocab_delta.json 对应条目 + 跑一次
`heal_source` 即逐字节还原源文件（journal 已有完整记录支撑回退）。

### 词表二轮进化（决策 2，`--proposer-policy max_marginal`，45 代全 archived，$0）

| 小说 | 基线 | 终态 | 降幅 | 保留 delta |
|---|---|---|---|---|
| 封神（留出） | 0.7847 | 0.1969 | **−0.588** | 402 条 |
| 水浒（内层） | 0.7653 | 0.6796 | −0.086 | 142 条（一轮） |
| 红楼（内层） | 0.8698 | 0.8302 | **−0.040** | 27 条 |
| 三国（留出） | 0.6573 | 0.6392 | −0.019（共享名溢出） | 0（未单独提议） |
| 西游（内层） | 0.8087 | 0.7942 | −0.015（溢出） | 0 |

要点：
- max_marginal 按"批次条数/地名总数"选最大的封神直到池尽（gen70-110），
  再自然轮到红楼（gen111-114）——**在线轨迹与回放器离线预测逐值一致**
  （封神 20 代后 0.4977，回放预测 0.4977），回放器有效性再添实证；
- anti-hack 二轮新增黑名单 21 条（红楼高频名 怡红院/潇湘馆/蘅芜苑 等 13 条 +
  封神/水浒数条；红楼 golden 覆盖密度高是红楼 delta 只有 27 条的主因）；
- §7.1 由此转正：内层集水浒 + 红楼两项冻结指标显著改善（>0.02，零噪声）。

### prompt 变异降级（证据链复测的诚实结果）

二轮后重跑证据链时，gen64 prompt 变异第三次复测 macro=0.4603 **低于基线 A**
（0.4665），三次测量均值差 +0.016 < 噪声底 0.03 → 按 §6.3 历史最优复测规则
**降级**：移出 Pareto 前沿、prompt_state override 置空、extraction_system.txt
逐字节还原（已验证与 6d7d2391 一致）、journal 记 `state-correction-s5`。
§5 并入清单中"prompt 三条规则并入（附条件）"相应改为**不并入**。

### 合并与 main 上验证（决策 3）

- main 合并前确认停在 6d7d2391 无新 commit ✅
- merge commit：**`ff34de34`**（--no-ff，41 文件 +15739/−12）
- main 上验证：全量测试 **1421 passed, 3 skipped**；quality_loop
  `main-post-merge-verify` **exit=0**
- 未 push

### 决策 4 落地

- 方法论 §8 新增"已排除的搜索方向：权重空间（阶段 2，33 代证据）"
  （docs/analysis/geo-self-evolve-methodology.md，随 merge 进 main）
- freeze 所有权规则写入 `backend/scripts/evolve/README.md`「冻结清单所有权规则」，
  CLAUDE.md「Development Process」节加一行指向

---

## 2. 正式证据链（2026-09-18 复跑）

### 2.1 系统级门禁（当前工作区 = 最优 genome：142 条词表 delta + gen64 prompt + 默认权重）

```
quality_loop.py --tag evolve-stage5-final  → exit=0（golden 16/16，各指标 delta +0.0000，"未见回退"）
benchmark_hierarchy.py --novel=xiyouji     → exit=0（errata 残留 31；规则引擎 recall 48.4%）
benchmark_hierarchy.py --novel=honglou     → exit=0（残留 23；recall 78.3%）
benchmark_hierarchy.py --novel=shuihu      → exit=0（残留 141；recall 74.5%）
```

注：benchmark_hierarchy 测的是 DB 中的层级现状（进化不改 DB，评估全部在 scratch/冻结副本上），其 gold errata 残留数与进化前一致——它证明的是"进化没有污染主数据流"，这正是 §6 安全规则的目标。

### 2.2 换种子/换序复测（`stage5_verify.py`，产物 `out/stage5_verification.json`）

| 指标族 | 复测方式 | 结果 | 判定 |
|---|---|---|---|
| topo（阶段 2，当前=默认权重） | 换章节顺序 seed 7/23 重建 | 最大抖动 0.0056/0.0209 | ✅ ≤ 基线噪声底+阈值 0.0309（v2.2 口径） |
| prompt recall（阶段 3 gen64 变异） | 冻结 15 章快速层重跑（$0.13） | macro 0.5074→0.4796，jitter 0.0278；单本最大 jitter 0.073（西游） | ✅ 在噪声底内（macro≤0.03，单本≤0.09），但**相对基线 +0.013 的下界低于 0.03 显著线**——方向正、幅度不确定 |
| geo.unresolved_rate（阶段 1） | 确定性复算 | 水浒 0.687036 与 journal 逐值一致 | ✅ 规则指标无噪声 |

---

## 3. 阴性结果正式分析（阶段 2：权重/参数级）

**假设**：层级质量可通过外置权重的局部扰动（11 个参数，±10-60%）在冻结指标上获得 >0.02 的显著改善。

**实验**：34 代 live 进化（jitter ±10-20% → 停滞后自动切 probe ±30-60%，轮询+动量，单参数归因），全部分代门禁+Pareto 记账，0 LLM 成本。

**证据**：
1. 平台期：jitter 前 5 代指标**零变化**（连结构护栏都不动）——管线对小幅权重扰动不敏感（LLM base parents + name-containment + prior override 三个高优先通道遮蔽了票数变化）；
2. 权衡墙：所有产生变化的探针（gen31/33/37/41/44）都是"水浒涨、红楼崩"或反向，分小说无回归约束下无一通过；
3. 噪声底：默认参数自身换序抖动 0.0209（红楼 precision），与两次被接受改善的幅度（+0.0048）同数量级——被接受项均被 §6.3 复测正确降级；
4. 回放器确认：33 个已实现动作对动作空间（11 参数 × 2 方向 × 2 机制 × 父代态）太稀疏，选择策略间无法分辨高下（UNEXPLORED 主导）。

**结论**：默认权重在"分小说无回归 + 噪声底 0.02"约束下是 Pareto 最优（或极接近）。层级质量的杠杆不在权重而在**证据质量**（抽取 prompt）与**词表覆盖**——这与阶段 1/3 的阳性结果方向一致。

**对主线的建议**：不要为此调权重；若要提升层级指标，优先 (a) 扩大 golden 标注量以压低噪声底（当前 ~50-130 地点/本，抖动 ±0.02）、(b) 投 prompt/词表通道。

---

## 4. 基因位/机制有效性分析（§7.6）

| 基因位 | 投入 | 产出 | 单位成本产出 |
|---|---|---|---|
| L1 词表/字典（ACE delta） | 65 代（两轮）/ $0 / ~620s | 水浒 −0.086 / 红楼 −0.040 / 封神 −0.588 / 三国 −0.019（溢出）（全部显著、无噪声、复算一致）+ 571 条坐标字典 | **最高**（零成本 + 确定性收益） |
| L3 prompt（GEPA 反思） | 15 代 / $1.89(+基准 $0.56+复测 $0.25) / 1007s | macro recall 三次测量 0.5074/0.4796/0.4603 vs 基线 0.4665/0.4466——**不可复现，已降级**；产出：GEPA 闭环可行性与噪声底数据 | 低（本轮收益不可信；杠杆在压低噪声底后重试） |
| L2 权重/参数 | 34 代 / $0 / 180s | 阴性结果（默认即 Pareto 最优）；产出噪声底数据 + 外置钩子 | 无直接收益，但排除了一整片假设空间 |

**机制层面**：
- ACE 增量 delta + Curator 去重：零事故，571 条全部可追溯（条目级 generation/ancestor/frequency provenance），抽检通过（§1.5）；
- anti-hack 黄金集检测：词表 79 条黑名单（含红楼高频名 怡红院/潇湘馆 等 13 条——golden 密度高是红楼 delta 少的主因）+ prompt 1 次拦截（gen55"两界山"），防泄漏实证有效；
- 分小说记账门禁：拦下全部 21 次灾难性遗忘候选；
- §6.3 复测降级：拦下 2 个噪声级伪改善（阶段 2）+ 降级 1 个 LLM 噪声伪改善（阶段 3 gen64，证据链复测三次测量不可复现）——复测机制在 LLM 指标上同样必要；
- 停滞护栏 + 机制切换：阶段 2 自动切换 jitter→probe（打破平台期获得真实信号），阶段 3 停滞警示入轨迹（打破提议器同质循环，3 代后产出唯一被接受变异）；
- **回放器策略证据（阶段 4）**：词表进化若用 max_marginal 策略，达同等总降幅只需 6 代（省 70% 预算）；round_robin/ucb1 需 11 代（省 45%）。已接 `--proposer-policy`。

---

## 5. 并入方案建议

| 成果 | 建议 | 理由 / 风险 / 回退 |
|---|---|---|
| 571 条词表 delta（geo_resolver.py 定界块） | **并入（已并入）** | 水浒 −0.086 / 红楼 −0.040 / 封神 −0.588 确定性收益（规则指标零噪声）；抽检通过（§1.5）；风险=坐标为"最近已解祖先"近似值；回退=清空 vocab_delta.json 后 heal 重渲染（逐字节还原，有测试锁定） |
| prompt 三条规则（extraction_system.txt 2b/2c/2d） | **不并入（已降级回退）** | 阶段 5 复测不可复现（三次测量 0.5074/0.4796/0.4603 vs 基线 0.4665/0.4466，均值差 +0.016 < 噪声底 0.03）→ 按 §6.3 降级，文件已逐字节还原（见 §1.5）。规则思路留档于 journal gen64 备查 |
| 11 个参数外置钩子（evolve_params.py + 3 文件 11 处注入点） | **并入** | 默认关=行为逐字节不变（实测重建指标逐值一致 + 1421 测试全绿）；为未来调参/灰度实验留通道；风险=近乎零；回退=删钩子恢复字面量 |
| evolve/ 基建（run_loop/geo_vocab/weight_jitter/prompt_evolve/replay/fixtures/测试） | **并入** | 自包含于 scripts/evolve/，不触生产路径；冻结清单机制对主线也有防护价值；风险=仓库体积（fixtures ~200KB）；回退=整目录删除 |
| 噪声底数据与 eval_policy 口径 | **并入（随基建）** | 是后续一切质量实验的显著性基准 |
| 阶段 2 权重实验记录 | **留分支**（journal 已含全部 lineage） | 阴性结果有存档价值，但 weights_state.json 的 overrides 为空，无并入物 |
| out/ 运行产物（dashboard、review、replay_result） | **留分支/不入库** | gitignored 维持现状 |
| 阶段 0-2 的 frontier.json 等中间态 | **丢弃**（可再生） | replay/backfill 可从 journal 重建 |

### src 改动的生产行为变化明细（最终态：prompt 已还原，剩两处）

1. **`geo_resolver.py` 定界块**（`_SUPPLEMENT_GEO.update({...})`）：生产行为变化=571 个地名（水浒 142 + 封神 402 + 红楼 27）从"未解析"变为"解析到最近祖先坐标"。影响面：地图管线的坐标解析（这些地点此前由 `place_unresolved_geo_coords` 在布局时近似放置，现在提前到解析层、确定性更强）。测试证据：全量 1421 passed；geo 复算与 journal 逐值一致。
2. ~~`extraction_system.txt` prompt~~ **（已撤销）**：阶段 5 复测不可复现，按 §6.3 降级后文件与 6d7d2391 逐字节一致，无生产行为变化。
3. **`evolve_params.py` + 11 处钩子**：生产行为**逐字节不变**（默认关）；证据=钩子合入前后五本重建指标逐值一致 + 全量测试。仅在设 `EVOLVE_PARAMS_JSON` 环境变量时生效（仅进化评估子进程使用）。

---

## 6. 决策点执行记录（用户授权主代理拍板，已全部执行）

1. **词表 delta 并入** → 已执行：程序化抽检通过（§1.5，15 条明细见表；3 条语义边界个案已注记待用户复核），随 merge 并入。
2. **prompt 变更** → 证据链复测不可复现，按 §6.3 降级回退，**不并入**；若未来重启 prompt 级，先做 golden 扩标注/M5 校准压噪声底。
3. **权重阴性结果登记** → 已执行：方法论 §8「已排除的搜索方向：权重空间」。
4. **freeze 所有权** → 已执行：README「冻结清单所有权规则」+ CLAUDE.md 指向行；维护责任=任何修改清单内文件的人（规则强制，CI 拦得住）。
5. **下一阶段预算** → 词表二轮已执行（45 代，$0，封神/红楼达标）；**prompt 第二轮本任务未做**，留作并入后的独立任务（预估 ~$2）。
