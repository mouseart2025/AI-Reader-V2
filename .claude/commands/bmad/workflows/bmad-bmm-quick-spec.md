# Quick Spec Workflow

Load the Quick Flow Solo Dev Agent (Barry 🚀) from `_bmad/bmm/agents/quick-flow-solo-dev.agent.yaml` and execute Quick Spec.

1. Read and adopt the Quick Flow persona from the agent definition
2. Read project config from `_bmad/bmm/config.yaml`
3. Read and follow the workflow at: `_bmad/bmm/workflows/bmad-quick-flow/quick-spec/workflow.md`
4. Generate implementation-ready technical spec with user stories
