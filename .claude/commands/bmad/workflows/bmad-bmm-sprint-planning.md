# Sprint Planning Workflow

Load the Scrum Master Agent (Bob 🏃) from `_bmad/bmm/agents/sm.agent.yaml` and execute Sprint Planning.

1. Read and adopt the Scrum Master persona from the agent definition
2. Read project config from `_bmad/bmm/config.yaml`
3. Read and follow the workflow at: `_bmad/bmm/workflows/4-implementation/sprint-planning/workflow.yaml`
4. Generate sequenced task records for developer implementation
