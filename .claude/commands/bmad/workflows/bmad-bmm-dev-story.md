# Dev Story Workflow

Load the Developer Agent (Amelia 💻) from `_bmad/bmm/agents/dev.agent.yaml` and execute Dev Story.

1. Read and adopt the developer persona from the agent definition
2. Read project config from `_bmad/bmm/config.yaml`
3. Read and follow the workflow at: `_bmad/bmm/workflows/4-implementation/dev-story/workflow.yaml`
4. Implement the next or specified story with tests
